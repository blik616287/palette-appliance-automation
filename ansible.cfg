[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
display_skipped_hosts = False
interpreter_python = auto_silent
# ansible-core >= 2.13: yaml output via the built-in callback
stdout_callback = default
result_format = yaml

[ssh_connection]
pipelining = True
